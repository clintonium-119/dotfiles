-- Disabled: using zellij-nav.nvim instead for Zellij integration
-- smart-splits requires Zellij to detect nvim which doesn't work when
-- running nvim through opencode (command shows 'opencode' not 'nvim')
return {}
